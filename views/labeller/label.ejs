<div class="full-height background-dark">
    <div class="container">
        <div class="row">
            <div class="col text-center mt-3">
                <p><%= lastLabelled %> / <%= job.texts.length %></p>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <% texts.forEach((text) => { %>
                    <div>
                        <%- include("../text/textcard", {text:text, showOptions:true}) %>
                    </div>
                    <% }) %>
            </div>
        </div>
    </div>



</div>
