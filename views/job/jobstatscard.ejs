<div class="card my-2">
	<div class="card-header text-capitalize font-weight-bold">
		Summary Stats
	</div>
	<div class="card-body">
		<h6 class="card-title"></h6>
        <div>
            <strong>Total Labellers:</strong> <%= labellerStats.complete ? labellerStats.complete : 0 + labellerStats.inProgress ? labellerStats.inProgress : 0 %> |
            <span class="font-italic text-muted"><%= labellerStats.inProgress ? labellerStats.inProgress : 0 %> in progress, <%= labellerStats.complete ?? 0 %> complete.</span>
        </div>
		<div><strong>Progress:</strong> <%= dataStats.numLabelledData %> of <%=job.texts.length %> data points have at least <%= job.numLabels %> labels.</div>
        <div><strong>Total useful data points:</strong> <%= dataStats.numUsefulData %> </div>
        <small class="text-muted font-italic"><%= dataStats.numUsefulData %> data points have decisive labels i.e. more than half of the labels for these data points have the same sentiment.</small>
        <!-- CLIENT or ADMIN  -->
        <div>
            <% if(currentUser.userType!==1){ %>
                <a href="/client/download/<%= job._id %>" target="_blank" 
                    class="btn btn-secondary mx-1 float-right btn-sm">Download Results</a>
            <% } %>
        </div>
	</div>
</div>